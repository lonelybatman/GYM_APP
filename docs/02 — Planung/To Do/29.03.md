
 1. Wurde output.sql tatsächlich in Supabase eingespielt?
  Kontrollieren: Gibt es Daten in exercise, equipment_option, exercise_perspective?
  2. exercise_perspective Table — wird vom Import-Script erstellt, aber wird sie in der App überhaupt schon verwendet? Die 3D-Model TODOs
  (Kamera-Perspektiven je nach Exercise) wären der Konsument davon.
  3. TRUNCATE-Reihenfolge im Script (Zeile 194-195):
  TRUNCATE equipment_option;
  DELETE FROM exercise;
  4. Das fehlt noch: set_log und plan_exercise werden hier nicht gecleart, aber laut Memory-Note war der Fix TRUNCATE set_log, plan_exercise,
  equipment_option CASCADE. Im aktuellen Script steht das nicht — könnte beim Re-Import FK-Fehler geben wenn plan_exercise/set_log Daten enthalten.
  5. Muscle groups Abs und Lat — sind die in der App (MUSCLE_DATA / step3.tsx Mapping) berücksichtigt? Das Mapping hat nur Shoulders → Shoulder und Glutes →
   Glute, aber Abs und Lat könnten auch Diskrepanzen haben.