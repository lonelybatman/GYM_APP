### Kurzfassung: Was wir wie gemacht haben

1. `exercise1.csv` + Exercise Builder (Triceps, Free + Cable)  
    Aus CSV und `import_exercises.js` / `config-utils` / `[exerciseId].tsx` abgeleitet:
    
    - extension: Standard-Attachment nooses, u. a. 1 Hand, Neutralgriff, hohe Kabellage, Stand 0°, ein Kabel.
    - overhead extension: wieder nooses, u. a. 2 Hands, Neutral, mittlere Kabellage, Stand 180°, ein Kabel.
2. Superdefault und „d-handle fix“ (Zeilen 1–70)
    
    - pushdown: `d-handle fix` nur FALSCH → nicht sinnvoll nutzbar.
    - extension: im Superdefault-Block wählbar.
    - overhead extension: in den kurzen Superdefault-Blöcken nicht; Zeile 61 macht `d-handle fix` nach Import-Zusammenführung doch verfügbar.
3. HumanModel3D  
    Bestätigt: 3D-Modell im Builder, Pose/Bank-Logik; später mehr Touch möglich.
    
4. Plan „Pose → Muskelaktivierung“  
    Entwurf: Schulter-Ebenen nicht überflüssig; Pose-Schicht (`ArmKinematicState`), Builder + Touch, Aktivierung zuerst Lookup/Regeln, optional Momentarme; Verweis auf bestehende Dateien und `exercise_muscle_activation`. Umsetzung = offen (To-dos im Plan-Frontmatter).
    

Gespeichert unter:  
`C:\Users\kubis\.cursor\plans\pose_zu_muskelaktivierung_87edd693.plan.md`  
(Das liegt im Cursor-Plans-Ordner, nicht automatisch im Git-Repo `gym-app`.)