- 3D Exercise Builder (`HumanModel3D`)
    
    - Vollbild-Flow stabilisiert (nur ein aktiver GL-Kontext, kein Konflikt zwischen Preview und Modal).
    - Bank-Verschiebung im Vollbild verbessert.
    - Steuerkreuz (D-Pad) fix unten im Modal positioniert, damit es zuverlässig sichtbar ist.
    - Toolbar/Leiste für Bank-Bewegung im Modal besser platziert.
    - `setupGL`-Abhängigkeiten korrigiert (inkl. `bench`), damit Änderungen sauber neu gerendert werden.
- Perspektiven-Logik (`exercise-builder/[exerciseId].tsx`)
    
    - Für `bench_cable` und `free_cable` wird `oben` zuverlässig angeboten, wenn nötig.
    - Umschalten auf `oben` für Bank-Adjust-Flow eingebaut/verbessert.
    - `allowBenchXZEditor` für passende Kombinationen aktiviert.
- Step 3 Add Exercise Flow (`create-plan/step3.tsx`)
    
    - Place/Weight Type von Switch-Toggles auf Dropdown-Auswahl per Klick umgebaut.
    - Layout links/rechts beibehalten: Place links, Weight Type rechts.
    - Dropdown als Overlay statt Push-Layout, damit Liste nicht nach unten springt.
    - Hintergrund/Farben der Dropdowns opak gemacht (kein „Durchscheinen“).
    - Z-Index/Elevation mehrfach nachgeschärft.
- Dropdown-Finalfix
    
    - Dropdown in eigene Modal-Overlay-Ebene ausgelagert, damit es garantiert ganz vorne liegt.
    - Dadurch verschwinden die Probleme mit Übungstexten „über“ dem Dropdown.
- Navigation-Rücksprung verbessert
    
    - Beim Öffnen einer Übung aus Step3 werden Filter (Muskel, Place, Weight Type, etc.) mitgegeben.
    - Beim Zurück aus der Übung landet man wieder im Add-Exercise-Modal mit den zuvor gesetzten Filtern.
    - Fehler „Maximum update depth exceeded“ gefixt, indem die Restore-Logik nur einmal pro Rückkehr-Signatur angewendet wird.
- Expo/Startproblem
    
    - Startproblem analysiert: Port `8081` war belegt.
    - Expo erfolgreich auf `8082` gestartet (`npx expo start --port 8082`).