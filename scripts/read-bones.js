  const fs = require('fs');
// This will be read from the Expo document directory
// Run: npx expo export --platform android then check the logs
// Or check via adb: adb shell cat /data/data/com.gymapp/files/bones.txt
console.log('Run the app first, then check Expo logs for bone names');
